#include <stdlib.h>
#include <stdio.h>
/**
 * main- gedel geba
 * ehe mn larg
 * Return:0 gedelegn
 */
int main(void)
{
int ch;
for (ch = 48; ch <= 57; ch++)
{
putchar(ch);
}
putchar(10);
return (0);
}
