#include <stdlib.h>
#include <time.h>
#include <studio.h>
/**
 * main -entry
 *
 * Return: always 0
 */
int main(void)
{
int n;
srand(time(0));
n = rand() - RAND_MAX / 2;
if (n > 0)
{
printf("%d is %s\n", postive);
}
else if (n < 0)
{
printf("%d is %s\n", negative);
}
if (n > 0)
{
printf("%d id %s\n", "zero");
}
return (0);
}

